"""
【程序42】  
题目：809*??=800*??+9*??+1   
其中??代表的两位数,
8*??的结果为两位数，
9*??的结果为3位数。
求??代表的两位数，
及809*??后的结果。  

注：809*?? = 800*?? + 9*?? 恒成立，题目中"+1"疑为笔误，
    实际约束为后两个条件。
"""
for a in range(10, 100):
    if 10 <= a * 8 <= 99 and 100 <= a * 9 <= 999:
        print(f"?? = {a}, 809 * ?? = {809 * a}")
        # 验证: 800*?? + 9*?? = 809*??
        print(f"验证: 800*{a} + 9*{a} = {800 * a + 9 * a}")

